## 2025-02-14 - Add aria-labels to components\n**Learning:** Icons and utility buttons that lack textual content but convey meaningful actions (like 'Edit' or 'Mark all read' buttons) can fail screen reader evaluations if not explicitly labelled.\n**Action:** Always provide `aria-label` attributes to button elements lacking explicit text or textual content (like when an icon is present instead).
## 2025-02-14 - Update dependency and verify version locking in tests\n**Learning:** In order to prevent the regression of dependency versions over time, there's a strict pattern where all dependencies updated for security must also be updated in `apps/web/tests/dependency-security.test.ts` to enforce the version limit through CI tests.\n**Action:** When updating a dependency version in , always check  for assertions and add tests if missing.
## 2025-02-14 - Update dependency and verify version locking in tests\n**Learning:** In order to prevent the regression of dependency versions over time, there's a strict pattern where all dependencies updated for security must also be updated in apps/web/tests/dependency-security.test.ts to enforce the version limit through CI tests.\n**Action:** When updating a dependency version in pnpm-workspace.yaml, always check dependency-security.test.ts for assertions and add tests if missing.
