## 2025-02-14 - Add aria-labels to components\n**Learning:** Icons and utility buttons that lack textual content but convey meaningful actions (like 'Edit' or 'Mark all read' buttons) can fail screen reader evaluations if not explicitly labelled.\n**Action:** Always provide `aria-label` attributes to button elements lacking explicit text or textual content (like when an icon is present instead).
