## 2024-05-22 - React Imports
**Learning:** React hooks like useMemo must be imported directly or via React namespace, but checking imports is crucial to avoid reference errors. Also, complex lists should be componentized and memoized.
**Action:** Always verify imports after refactoring and use separate components for list items.
