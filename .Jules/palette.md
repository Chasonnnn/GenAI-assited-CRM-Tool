## 2025-05-18 - Missing ARIA Labels on Buttons
**Learning:** Found multiple instances where interactive elements like `button` and `div role="button"` lack `aria-label` attributes.
**Action:** Always verify that interactive elements have appropriate ARIA attributes.
