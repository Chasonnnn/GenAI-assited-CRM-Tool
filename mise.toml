min_version = "2026.7.16"

[tools]
# Compatibility exception: production Docker and CI currently use Python 3.11.
# Move this to the global 3.14.6 baseline only with the production-runtime migration.
python = "3.11.14"
node = "24.18.0"
uv = "0.12.0"
pnpm = "11.18.0"

[settings]
lockfile = true
lockfile_platforms = ["macos-arm64", "linux-x64"]
