SELECT s.id, s.stage_id, s.status_label, s.owner_type, s.owner_id, s.created_at FROM public.surrogates AS s WHERE s.organization_id = $1 AND s.stage_id = $2 AND s.is_archived = FALSE ORDER BY s.created_at DESC LIMIT $3 OFFSET $4;
