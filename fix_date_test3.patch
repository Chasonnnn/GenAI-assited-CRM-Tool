--- apps/web/tests/change-stage-modal.test.tsx
+++ apps/web/tests/change-stage-modal.test.tsx
@@ -234,16 +234,9 @@
             .getAllByText(format(interviewDate, "d"))
             .map((element) => element.closest("button"))
             .find((button): button is HTMLButtonElement => Boolean(button) && !button.disabled)

-        if (!dayButton) {
-            fireEvent.click(screen.getByRole("button", { name: /go to next month/i }))
-            const nextMonthDayButton = screen
-                .getAllByText(format(interviewDate, "d"))
-                .map((element) => element.closest("button"))
-                .find((button): button is HTMLButtonElement => Boolean(button) && !button.disabled)
-            expect(nextMonthDayButton).toBeDefined()
-            fireEvent.click(nextMonthDayButton!)
-        } else {
-            fireEvent.click(dayButton)
-        }
+        expect(dayButton).toBeDefined()
+        fireEvent.click(dayButton!)

         expect(screen.getByRole("button", { name: "Save Change" })).toBeDisabled()

@@ -297,16 +290,9 @@
             .getAllByText(format(interviewDate, "d"))
             .map((element) => element.closest("button"))
             .find((button): button is HTMLButtonElement => Boolean(button) && !button.disabled)

-        if (!dayButton) {
-            fireEvent.click(screen.getByRole("button", { name: /go to next month/i }))
-            const nextMonthDayButton = screen
-                .getAllByText(format(interviewDate, "d"))
-                .map((element) => element.closest("button"))
-                .find((button): button is HTMLButtonElement => Boolean(button) && !button.disabled)
-            expect(nextMonthDayButton).toBeDefined()
-            fireEvent.click(nextMonthDayButton!)
-        } else {
-            fireEvent.click(dayButton)
-        }
+        expect(dayButton).toBeDefined()
+        fireEvent.click(dayButton!)

         fireEvent.change(screen.getByLabelText(/interview hour/i), {
             target: { value: "1" },
